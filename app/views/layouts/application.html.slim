doctype html
html lang="en"
  head
    title
      | Splitwise
    = render 'layouts/header_layout'
  - if ['sessions', 'registrations', 'passwords'].include?(controller_name)
    body.text-center
      main.w-100
        = yield
      = render 'layouts/jgrowl_notifications'
  - else
    body.d-flex.flex-column.h-100
      = render 'layouts/top_nav_bar'
      main.flex-shrink-0 style="margin-top: 65px; padding-bottom: 40px;"
        .container
          = yield
      = render 'layouts/jgrowl_notifications'