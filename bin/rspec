#!/usr/bin/env ruby
ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../Gemfile", __dir__)
ENV["BUNDLE_IGNORE_RUBY_VERSION"] = "true"
require_relative "../config/boot"
require "rspec/core"
RSpec::Core::Runner.invoke
